{
	auto_https off
}

:{$PORT} {
	root * /app/dist
	encode gzip
	file_server

	# SPA fallback
	try_files {path} /index.html

	# Correct MIME types for JS modules
	header /assets/*.js Content-Type application/javascript
	header /assets/*.mjs Content-Type application/javascript
}
